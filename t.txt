# src-tauri/Cargo.toml (فقط بخش های زیر را تغییر دهید)

[build-dependencies]
tauri-build = { version = "1.0", features = [] } # از 2.0.0-beta به 1.0 تغییر یافت

[dependencies]
# ... وابستگی‌های دیگر (ffmpeg-next و anyhow دست نخورده باقی می‌مانند) ...

# وابستگی های Tauri
tauri = { version = "1.6", features = [ # از 2.0.0-beta به 1.6 تغییر یافت
    "dialog-open",
    "fs",
    "wry",
    "dev" # برای حالت توسعه (dev)
] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
